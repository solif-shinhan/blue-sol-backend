name: Deploy to EC2

on:
  push:
    branches: [ "main" ]

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: "17"
          distribution: "temurin"

      - name: Grant execute permission for gradlew
        working-directory: blue-sol-backend
        run: chmod +x gradlew

      - name: Build with Gradle
        working-directory: blue-sol-backend
        run: ./gradlew clean build -x test

      - name: Create Google Vision credential file
        working-directory: blue-sol-backend
        run: |
          mkdir -p secrets
          printf "%s" "${{ secrets.GCP_VISION_CREDENTIALS_B64 }}" | base64 -d > secrets/gcp-vision.json

      - name: Login to DockerHub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build and push Docker image (PROD)
        run: |
          docker buildx build \
            --no-cache \
            --pull \
            --platform linux/amd64,linux/arm64 \
            -t tony0103/shinhan-backend:latest \
            -t tony0103/shinhan-backend:prod-${{ github.sha }} \
            --push \
            blue-sol-backend

      - name: Deploy to EC2 (PROD)
        uses: appleboy/ssh-action@v1.0.0
        with:
          host: ${{ secrets.EC2_HOST }}
          username: ec2-user
          key: ${{ secrets.EC2_SSH_KEY }}
          script: |
            set -e
            cd ~/app

            cat > .env << 'EOF'
            SPRING_PROFILES_ACTIVE=prod
            DB_URL=${{ secrets.DB_URL }}
            DB_USERNAME=${{ secrets.DB_USERNAME }}
            DB_PASSWORD=${{ secrets.DB_PASSWORD }}
            JWT_SECRET=${{ secrets.JWT_SECRET }}
            REDIS_HOST=${{ secrets.REDIS_HOST }}
            REDIS_PORT=${{ secrets.REDIS_PORT }}
            S3_ACCESS_KEY=${{secrets.S3_ACCESS_KEY}}
            S3_SECRET_KEY=${{secrets.S3_SECRET_KEY}}
            GOOGLE_APPLICATION_CREDENTIALS=/app/secrets/gcp-vision.json
            EOF

            docker compose pull backend-prod
            docker compose up -d --force-recreate backend-prod
            docker image prune -f